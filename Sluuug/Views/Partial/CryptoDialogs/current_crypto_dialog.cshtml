@model Slug.Model.CryptoChat
@using Slug.Extensions

<div class="cryptp_chat" id="@Model.GuidId" style="background-color:coral; cursor:pointer">
    <b>@Html.Translate("Text_Opening_Date") @Model.OpenDate</b>
    <span>@Html.Translate("Text_Status_Secret_Chat") @Model.ActiveStatus</span>
    <p>
        <span>@Html.Translate("Text_Secret_Dialog_Participants") </span>
    </p>
        <p>
            <span> @Model.UserCreator.Name </span>
            <img src="@Model.UserCreator.AvatarPath" height="30" width="30" />
        </p>
    <span class="last_msg_crypto">@Model.LastMessage</span>
</div>
