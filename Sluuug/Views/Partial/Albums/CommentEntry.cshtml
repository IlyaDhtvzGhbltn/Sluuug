@foreach (var item in Model) {
<div class="comment_item">
    <a href="/private/user/@item.UserPostedID"><img src="@item.UserPostedAvatarResizeUri" /></a>
    <span>@item.DateFormat</span>
    <span>@item.UserName  @item.UserSurName</span><p>
        <b>@item.Text</b>
    </p>
</div>
}